name: Deploy to Vercel

on:
  push:
    branches:
      - master
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Auto-approve required install scripts
        run: |
          npm ci --ignore-scripts
          if npm help approve-scripts >/dev/null 2>&1; then
            npm approve-scripts unrs-resolver
          else
            echo "npm approve-scripts not available on this runner; continuing."
          fi

      - name: Link Vercel project
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          rm -rf .vercel
          vercel link --yes --project my-day --scope mvdwalt6-collabs-projects --token "$VERCEL_TOKEN"

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel deploy --prod --scope mvdwalt6-collabs-projects --token "$VERCEL_TOKEN"
